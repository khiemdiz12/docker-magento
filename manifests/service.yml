apiVersion: v1
kind: Service
metadata:
    name: "teklegioncluster-b5bc"
    labels:
        app: "teklegioncluster-b5bc"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "teklegioncluster-b5bc"